# CLAUDE.md

heritage-crawler 固有の文脈。全プロジェクト共通の規約はグローバル CLAUDE.md に従う。

## このリポジトリの目的

国指定文化財等データベース (https://kunishitei.bunka.go.jp/bsys/index) から
**全データ**を抽出し、GitHub に記録するクローラー本体。対象は検索フォームの
`register_sub_id` が提供する**全 19 分類** — 建造物・美術工芸品・民俗文化財・
無形文化財・選定保存技術・記念物・文化的景観・世界遺産 (ADR 0025)。分類の定義は
`catalog.TARGET_CATEGORIES` が正本。

データの出力先は**文化財の種別ごとの別リポジトリ** (ADR 0001 / ADR 0009 /
ADR 0012 / ADR 0025)。`code4heritage` org 配下の **26 個**で、対応表は
`catalog.TARGET_DATASETS` が正本。**データが存在しないと分かった器は残さない**
(ADR 0013) — 「重要」の付かない伝統的建造物群保存地区は市町村が決定するため
データベースの対象外で、そのリポジトリは削除した。

## データソースの実態 (2026-08-11 実地調査。詳細は Issue #1 のコメント)

- 取得は 2 段構え。CSV で台帳を取り、`(台帳ID, 管理対象ID)` から
  `/heritage/detail/{台帳ID}/{管理対象ID}` を組み立てて詳細を取る (ADR 0002)。
  **リンクを辿る探索クロールは不要**
- CSV と詳細ページは相補的。CSV にしか緯度経度が無く、詳細にしか解説文・
  構造及び形式等・員数などが無い。両方を統合しないと全項目が揃わない
- CSV は棟単位に展開済み。検索一覧の 1:N (複数棟の指定) を自前で解く必要はない
- **取得対象は約 37,000 件** (2026-08-23 に全 19 分類の台帳を取って確定)。
  棟や件に展開されるのは 102 (1 指定 : 約 2.1 棟) と 201 (1 指定 : 約 1.05 件) だけで、
  残る 17 分類は指定 = 1 行。1 req/s で約 10 時間、生 HTML は gzip 圧縮で
  約 400 MB になる (ADR 0011)。
  **ここに書いた件数は規模感のための写しで、正本は README の生成表**
  (`render-readme` が書き出したデータから作り直す)
- **分割軸は分類ごとに違う** (下の「非自明な注意」)。都道府県で足りるのは
  101 / 102 / 103 / 201 / 211 / 202 / 301 / 311 / 401 / 411 / 412 / 901 まで
- 公共サイトが相手なのでレートに上限を設け、User-Agent に連絡先を記載する。
  **上限 1 req/s を超えると相手は 200 でエラーページを返す** (4 req/s で応答の
  15% が壊れた。ADR 0011)。`Accept-Encoding: gzip` も送るがサーバは返さない (実測)
- **1 req/s を守っていても 504 が返る時間帯がある。** 2026-08-12 の 23:47 JST に
  `/bsys/searchlist` が 3 回とも 504 (CloudFront が origin のタイムアウトを返した)。
  同じ検索が 18 秒で通る時間帯もあるので、相手の混み具合による。HTTP クライアントの
  再試行 (2 秒・4 秒) では抜けられないため、**週次は `scripts/retry.sh` で
  5 分空けて繰り返す** (取得済みを飛ばして再開するので相手への総量は増えない)
- 初回の全件取得はローカル、以降の差分更新は GitHub Actions の週次実行 (ADR 0006 / ADR 0020)。
  **生 HTML はキャッシュしてリポジトリにはコミットしない**

### 非自明な注意

- **CSV の POST パラメータは検索応答 HTML の csv-list フォームの hidden 値を
  そのまま送る。推測して組み立てると 504 になる** (調査時に CSV 機能の障害と
  誤認しかけた)
- CSV は **UTF-8 (BOM 付き)**。Shift_JIS ではない
- **CSV の 12 列目は見出しも意味も分類で変わる** (#74)。`都道府県` (101 系) /
  `所有者住所（所在都道府県）` (美術工芸品) / `地域` (無形文化財) /
  `都道府県、地域` (無形民俗文化財)。18 列を厳密比較すると 4 分類で必ず失敗するので、
  この列だけ既知の見出しを許す (`ledger.AREA_COLUMN_LABELS`)。**検索結果一覧の
  見出しは全分類で同じ**なので、そちらに分岐は要らない
- **検索結果が 0 件のときは csv-list フォームごと HTML に出ない**。CSV を要求せず
  0 件として次へ進む (推測して POST すれば上記の 504 を踏む)
- 検索結果の件数表示は**指定**単位、CSV の行数は**棟**単位。**件数表示どうしの引き算で
  網羅性を判定してはいけない** — 地域をまたぐ重複と取りこぼしが相殺して消える
  (401 の取りこぼし 1 件が「重複 33 件」に化けていた。#28)。全国件数と直接比べられるのは
  CSV の `(台帳ID, 管理対象ID)` の異なり数で、棟に展開されない分類 (102 以外) に限る
- 都道府県の分割軸には `２県以上` (全角２) と `地域を定めない` も含める。
  除くと都道府県で引けない指定を取りこぼす
- **分割軸は分類ごとに違う** (#74)。都道府県で引ける分類ばかりではない。
  分類が持つ `AreaScope` から `catalog.search_areas` が開く。
  **select に option があることと、その値で引けることは別** — 303 / 313 の
  select には地域の 9 区分が並ぶが、データ側の地域欄は**全件空**で 1 件も引けない。
  全国 1 回で取るのは 303 / 313 / 323 / 312 / 304 の 5 つで、いずれも全国 CSV が
  2,000 件未満 (504 にならないことを実測済み)。302 / 322 は都道府県 + 9 区分
- **seat_pref は格納値の完全一致**で絞る。101 には都道府県が未正規化のまま
  (`98` / `1`) 入った行が 3 件あり、どの option でも引けない。`catalog.IRREGULAR_AREAS`
  で拾っている。この種の取りこぼしは、地域合計と全国件数の差で気付ける
- **都道府県が空の行はどの seat_pref でも引けない** (空を送ると全国検索になるため
  `IRREGULAR_AREAS` の手が使えない)。401 に 1 件実在した。**2,000 件超の CSV 出力は
  504 になる**ので全国 CSV での代替も効かない。検索結果一覧から回収する (ADR 0017)
- **一覧のページ送りは `page_no` ではなく `pageNumber`。** `page_no` は常に 1 のまま
  送られる飾りで、変えても 1 ページ目が返る。表示件数は `pageSize` (最大 100)。
  一覧の行は緯度経度も持つ (地図表示ボタンの `mapChange(...)`。CSV の値と完全一致)
- **詳細ページ URL の第 1 セグメントは分類コードで、CSV の台帳ID ではない** (#74)。
  取得対象の 4 分類はこの 2 つが同値なので取り違えても表に出ないが、**台帳ID には
  複数の分類が同居する** — 401 に 401・411 (登録記念物)・412 (重要文化的景観)、
  303 に 303・323・313 など。台帳ID で組むとエラーページが返るので、URL も
  詳細キャッシュの置き場も分類コードで決める (`catalog.detail_url`)。
  検索結果一覧の詳細リンクも分類コード (`ListingRow.category_code`)
- CSV 出力はサーバ側セッション + CSRF に依存するが、詳細ページはセッション不要。
  素の HTTP クライアントで足り、ヘッドレスブラウザは要らない
- トップページのお知らせにある 2,000 件制限は一時的な障害であり恒久仕様ではない
  (ただし 2026-08-12 時点でも 401 の全国 CSV = 3,281 件は 504。当面は超えない
  分割で取る)
- **画像は取得・再配布しない** (作品毎に個別許諾が必要。ADR 0007)
- 文字情報は出典表示のもとで再配布できる (ADR 0007)。出力データには出典表記を
  必ず付す。コードは MIT、データは文化庁の利用規約と、ライセンスは 2 層に分ける
- **分類コードと出力先リポジトリは 1:1 でない** (ADR 0009 / ADR 0012)。102 は詳細
  ページの「国宝・重文区分」で 2 つに、401 は `種別１` / `種別２` で 6 つに分かれる。
  区分が読めないときの行き先は分類で違う — 102 は受け皿 (重要文化財) へ送り、
  **401 はどこへも書かず報告に出す** (受け皿を置くと誤りが史跡に紛れる)
- **401 には種別を 2 つ持つ複合指定がある** (旧浜離宮庭園 = 特別名勝 + 特別史跡)。
  両方のリポジトリへ同じ行を書く (ADR 0012)。6 リポジトリの行数の合計は 401 の
  指定件数を上回るので、突き合わせは `(台帳ID, 管理対象ID)` の異なり数で見る
- **`detail_rellist_*` モーダルの中身は分類で変わる。** 建造物系は附指定
  (`附名称` / `附員数`)、401 は指定等後に行った措置の履歴 (`異動年月日` /
  `異動種別1`〜`3` / `異動内容`。数字は半角)、無形文化財と選定保存技術は
  **保持者・保持団体** (誰が認定されているかは主情報に出てこない)、美術工芸品は
  **一つ書** (まとめて指定されたものの内訳)。分類で決め打ちせずラベルで見分ける。
  **`ト書` は主情報と一つ書の両方に出る**ので、振り分けは `一つ書主名称` の有無で決める
- **世界遺産 (901) のキーは他分類と重ならない。** 同じ物件が構成資産として他の
  リポジトリにも入るが、901 は**別の台帳に立った別のレコード**で、構成資産は
  1 本の文字列 (`二荒山神社、東照宮、輪王寺`)。401 の複合指定と違って
  `(台帳ID, 管理対象ID)` では名寄せできず、名称での突き合わせも 244 個中 62 個しか
  当たらないうえ誤爆する (「東照宮」は日光のほかに全国 6 か所)。**結び付けない**
  (ADR 0025 / [heritages#28](https://github.com/code4heritage/heritages/issues/28))
- **サンプル 1 件では対応表を作れない。** 各分類 1 件で作った対応表に、全件を
  読み直したら 12 種類・24,190 件の未知ラベルが残っていた (#74)。分類を足したら
  `scripts/` ではなくキャッシュを全部読んで集計する

## 現状

要件議論の主要な決定は ADR 0001〜0025 に記録済み。実装は 2 段構えの取得層
(台帳・詳細) と解析・出力層まで入っており、ADR 0021 (削除された指定の記録) も
記録の器と実在の確認まで入っている (#60 / #61)。

**3 つの持ち場 (クローラー・データリポジトリ 26 個・heritages) の関係は
`docs/pipeline.md` が正本**。週次の一巡・誰が何を書き込むか・どこで何を確かめるかを
1 枚にまとめてある。以下はクローラー側の作りの詳細。

| モジュール | 役割 |
|---|---|
| `src/heritage_crawler/catalog.py` | 分類・地域の語彙、出力先リポジトリの対応表、詳細ページ URL の組み立て |
| `src/heritage_crawler/http.py` | レートに上限を設ける HTTP クライアントと発射時刻の共有 |
| `src/heritage_crawler/search_page.py` | 検索応答から件数と csv-list の hidden 値を取る |
| `src/heritage_crawler/ledger.py` | 台帳の取得手順、全国件数との突き合わせ、CSV 行の読み出し |
| `src/heritage_crawler/listing.py` | 検索結果一覧を全ページ辿って網羅性を確かめ、引けない指定を回収する |
| `src/heritage_crawler/detail.py` | 台帳から取得対象を作り、詳細ページを巡回する (エラーページの検出も) |
| `src/heritage_crawler/detail_page.py` | 詳細ページの HTML から原文の項目を読む |
| `src/heritage_crawler/record.py` | **出力スキーマの正本** (ラベル対応・日付・都道府県・欠損) |
| `src/heritage_crawler/export.py` | キャッシュを走査して種別リポジトリごと・都道府県ごとの JSON Lines を書く (差分更新では前回の出力も使う) |
| `src/heritage_crawler/update.py` | 前回の出力・前回の台帳と突き合わせ、取り直す対象を決める (週次) |
| `src/heritage_crawler/metadata.py` | データリポジトリの `meta.json` (出典・利用日・表示名・語彙・件数) |
| `src/heritage_crawler/status.py` | データリポジトリの `status.json` (確認日。週次だけが書く) |
| `src/heritage_crawler/cache.py` | CSV と生 HTML の置き場とマニフェスト (再開の判断) |
| `src/heritage_crawler/readme.py` | README の件数表を書き出したデータから組み立てる |
| `src/heritage_crawler/cli.py` | `fetch-ledger` / `report-ledger` / `compare-ledgers` / `audit-listing` / `fetch-detail` / `report-detail` / `build-records` / `update-records` / `render-readme` |

依存パッケージは増やしていない (標準ライブラリで足りる)。
**初回の全件取得は完走済み** — 建造物・記念物の 4 分類 (23,742 件) が 2026-08-12、
残る 15 分類 (13,348 件) が 2026-08-23。**37,090 件が 26 のデータリポジトリに
入っている** (`code4heritage` 配下)。**分類ごとの全国件数と一致している** — 901 の
佐渡島の金山だけは相手が途中までしか描けない (HTTP 500) ので、欠けている印付きで入る
(#107 / ADR 0030。下の「データベース側の不具合」)。
**201 の「どの地域でも引けない指定」は一覧から回収する** — 所有者住所が空の 92 件で、
`audit-listing --recover` が拾う (#83 / ADR 0026)。
**差分更新 (#10) は完了**
— Actions からの疎通・差分検出の方法 (ADR 0018 / ADR 0020)・差分の突き合わせと
出力のマージ (`update-records`)・週次ワークフロー (`.github/workflows/weekly.yml`)・
push 用の GitHub App の secret まで入っている。**push まで含む本番はまだ通って
いない。** ロードマップと残る論点は Issue #1 を正本とする。

**差分更新は「前回の状態」をデータリポジトリの JSON Lines から読む** (ADR 0018)。
キャッシュも生 HTML も Actions へ持ち回らない (Actions のキャッシュは 7 日で
エビクトされ週次には持たない。**台帳の CSV だけは artifact で持ち回る**。ADR 0020)。
取り直すのは新規・台帳の値が変わったぶん・データベース側の不具合の再確認 (ADR 0030)・
**毎週 1/52 の巡回**だけ — ソース側に更新日が無いため、詳細ページだけの項目
(解説文・員数など) の変更は取り直して比べるしか捕まえられない。**スキーマ変更時の
全件再組み立てはローカル**に残す (Actions は差分専用)。

### 差分更新の作り (#10 / ADR 0020)

- **台帳の値の変化は CSV 同士で見る。** まずファイルごとにバイト列を比べ、違った
  ファイルの行だけを `(台帳ID, 管理対象ID)` で突き合わせて 18 列すべてを比較する。
  **前回の台帳は artifact で持ち回る** (499 ファイル / 11 MB、保持 90 日)
- **件数では当たりを付けられない。** 増加と減少が同じ週に重なると数字が動かず、
  見逃す。台帳を取り直すこと自体が正確さの担保
- **並び順は当てにしない。** 相手先の返す行順が安定している保証は無いので、
  バイトが違ったら行をキーで突き合わせる (並びだけの違いは差分にならない)
- **追加と削除は台帳と出力の突き合わせで決める。** 前回の台帳が無い回でも働き、
  地域をまたいで移った行を「片方で消えてもう片方に現れた」と読み違えない
- 巡回の割り当ては `sha256((台帳ID, 管理対象ID))` を 52 で割った余り。**組み込みの
  `hash` は実行ごとに種が変わる**ので使えない (毎週ちがう 1/52 を取ってしまう)。
  枠は日本時間の ISO 週で決める
- **キャッシュが先、前回の出力は控え。** 取り直した 1 件が取得に失敗しても前回の行が
  残るだけで、失敗が行の消失にならない
- **消してよいのは網羅性が確かめられた分類だけ** (`CategorySummary.looks_complete`)。
  地域をまたぐ重複は取りこぼしではないので通す (これで消せなくなるのは行き過ぎ)
- 表示名は前回の `meta.json` を土台に、今回組み立てたぶんで上書きする。毎週 2,000 件
  ほどしか組み立てないので、今回ぶんだけで作ると `meta.json` が回ごとに揺れる
- 手元の全件で**詳細キャッシュ無しのマージが前回の出力と 1 バイトも変わらない**ことを
  確かめてある (415 ファイル)。生成物の決定性はここで実証している
- **落とす前に詳細ページで存在を確かめる** (ADR 0021)。台帳からの不在は消極的な
  証拠でしかない。**「必要な情報が足りません」は過負荷のときにも返る**ので
  (ADR 0011)、実在キーを検査の**前後**に 1 件ずつ引く**対照群**で時制を担保する。
  確かめられなかったものは落とさず翌週やり直す。**この検査はキャッシュに書かない**
  — `fetch_details` を流用すると消えた行が失敗として積まれ、`--retry-failed` が
  毎週叩き、連続失敗の打ち切りが誤発動する。**102 では網羅性を確かめられない**ので、
  ここが唯一の個別証拠になる
- **落とした行は各データリポジトリのルートの `removed.jsonl` に残す** (ADR 0021)。
  `data/` の下に置くと heritages の配信が止まる (`meta.json` に無いファイルとして
  弾かれ、サイトも通常のレコードとして読もうとする)。**状態型**なので並ぶのは
  「いま消えているもの」だけで、復活すれば行は消える。**`build-records` (全件の
  組み立て直し) は触らない** — キャッシュからは履歴を再現できず、書けば消える
- **データベース側の不具合は、行の印とルートの `source-issues.jsonl` で名指しする**
  (#107 / ADR 0030)。途中まで描かれたページから作った行には `source_issue` を付ける
  (meta.json の facets にも載り、サイトで絞れる)。一覧の `kind` は 3 つ —
  `truncated` (途中切れ・行あり)・`stale` (取り直しに失敗して前回の行のまま)・
  `unavailable` (一度も取れていない・行なし)。状態型で `first_seen` をキーで引き継ぎ、
  **載ったキーは `Reason.RECHECK` で毎週取り直す** (直れば消える)。**途中切れで前回の
  完全な行を上書きしない。** 書くのは `update-records` だけで、`build-records` は
  `removed.jsonl` と同じ理由で触らない (印は付く)。一覧の変化は利用日を動かさない
- **データが変わらない週も、確認日だけは各データリポジトリに残す** (ADR 0023)。
  利用日は「そのデータを取り出した日」なので行が動かなかった種別では据え置かれ、
  `meta.json` も 1 バイトも変わらない。動くのはルートの `status.json` の**確認日**
  (データベースを見にいった日) だけで、その週は「◯◯ に確認 (差分なし)」のコミットが
  1 つ立つ。**書くのは `update-records` だけ** — `build-records` に日付を持ち込むと
  全件の組み立て直しが実行日で揺れる。中身の変化だけを追うときは
  `git log -- data meta.json removed.jsonl source-issues.jsonl`。サイトの「最終確認」にも同じ日付を渡す
  (週次の最後に heritages を起こす)

**データ閲覧サイトは `code4heritage/heritages` に 1 つだけ置く** (ADR 0015)。
種別ごとに複製しない — 分けるのはデータであってコードではない (ADR 0001) うえ、
同じ棟が複数のデータセットに現れる複合指定が 114 件あり、種別ごとのサイトでは
表現できない。地図は地理院タイル (ADR 0016)。サイトはデータリポジトリの
`meta.json` を読んで種別ごとの差異を吸収するので、種別が増えても手を入れずに済む —
**ただし「場所に結び付かない種別」は別だった** (heritages
[#23](https://github.com/code4heritage/heritages/issues/23))。座標の欠けを配信の
関門にしていたこと・所在地を一覧の手掛かりにしていたこと・保持者が検索索引に
入っていなかったことは、いずれも 4 分類でだけ成り立っていた前提で、全 19 分類の
データが揃った時点で手を入れた。

**ZIP での配布とリリースノートも heritages が引き受ける** (ADR 0019)。**クローラーは
関与しない** — 週次でデータリポジトリへ push したあと heritages を起こせば、全部入りと
種別ごとの ZIP を 11 箇所のリリースへ置く。**`update-records` の計画を変更履歴に
使わない**のが要点で、あれは台帳ベースなので解説文・員数の変更が現れない。差分は
前回のリリースと今回のデータの突き合わせで出す。

### 取得層の作り (#6 / #7)

- 台帳のマニフェストは JSON 全体を書き直し、詳細のマニフェストは JSON Lines へ
  追記する。**件数が 2 桁違う** (204 件 対 23,742 件) ため書き方を変えてある
- 詳細ページは 1 件失敗しても止めない。記録して次へ進み `--retry-failed` で
  拾い直す。ただし**連続失敗が続いたら打ち切る** (相手が落ちているか弾かれている)
- **台帳も 1 地域取れなくても止めない** (ADR 0022)。取れたぶんはキャッシュに入り、
  取れなかったものがあれば `fetch-ledger` は 1 を返す。次の回が試すのは残りだけ。
  止めていた頃は、`retry.sh` で繰り返しても**毎回同じ地域で死んで 1 つも進めなかった**
  (2026-08-16 の週次実行)。検索応答が読めないときは待ちを挟んで 3 回まで粘る —
  **読めない応答は 200 で返る**ので HTTP クライアント側の再試行は働かない
- **全国件数も「取得済みは飛ばす」の対象** (ADR 0027)。取れているものを毎回叩き直すと、
  そこで新しく失敗しうる — 2026-08-23 の週次は 3 回目の試行で台帳を全 19 分類
  取り切ったのに、叩き直した **101 の全国件数 1 つ**が取れずに 1 週ぶんの更新が飛んだ
  (#92)。ソース側の件数が動いたかを見たいときは `--force`
- **HTTP 200 でもエラーページのことがある** (ADR 0011)。取得層が本文を見て弾き、
  キャッシュに残さず失敗として記録する。これが無いと打ち切りも再試行も働かない。
  紛れ込んだぶんは `fetch-detail --recheck-cache` で通信せずに検査して取り直せる
- **5xx でも途中まで描かれたページのことがある** (#107 / ADR 0030)。相手はページを
  組み立てる途中で落ちると、そこまでの HTML にエラー画面をつないで 500 で返す。
  主情報の終わり (`heritage_detail END`) とエラー画面への切り替え (`/bsys/error`) が
  **両方**あれば本文を残し、`DetailEntry.issue = "truncated"` の印を付ける
  (`FetchError` が最後の応答の本文を持つ)。存在しない ID の 500 はエラー画面だけなので
  当たらない。**連続失敗には数える。** `--retry-failed` は途中切れも取り直す
- **レートの上限を決めるのは間隔だけ** (ADR 0010)。`RateLimiter` はワーカーごとに
  発射時刻を予約するので、並列度を上げても上限は超えない。埋まるのは応答待ちの
  隙間で、増えるのは同時接続だけ。相手が遅くなれば実効レートは自ら落ちる
  (並列度 C・応答 R なら `min(1/間隔, C/R)`)
- **網羅性は `report-ledger` の「異なり」列で見る** (#28)。`summarize` は CSV を
  読んでキーの異なり数を数え、棟や件に展開されない分類
  (`Category.expands_to_buildings` が偽 = 102 / 201 以外) では全国件数と直接比べる。
  件数表示ベースの差 (`difference`) はこの 2 分類でしか判定に使えない
- **足りない指定を名指しするのは `audit-listing`** (ADR 0017 / ADR 0026)。検索結果
  一覧を全ページ辿ってキーを集め、台帳と突き合わせる。`--recover` を付けると、
  地域では引けない指定を一覧の行 (名称・緯度経度) から台帳へ回収して
  `cache/ledger/<分類>/recovered.csv` に書く。**回収では地域欄を写さない** —
  一覧の見出しは全分類で「都道府県」だが CSV の 12 列目の意味は分類で変わる
- **対象は `Category.audits_with_listing` が真の分類** (ADR 0026)。これは「一覧の
  キーが台帳のキーに載るか」で、展開の有無とは別。**偽なのは 102 だけ** — 一覧を
  27 ページ辿っても 1,907 行しか読めない (件数表示は 2,633。複数棟の指定が詳細ページへ
  リンクを持たない)。**201 は展開されるが一覧のキーは台帳に載る**ので突き合わせられ、
  「台帳にあって一覧に無い」571 件は 2 件目以降のぶんで指定解除ではない

### 解析・出力層の作り (#8)

- **スキーマの正本は `record.py` の `FIELD_KEYS`**、決定の根拠は ADR 0008。
  読み取り (`detail_page.py`) とスキーマ (`record.py`) を分けてあるので、
  サイト側の表記が変わっても直す場所は 1 つ
- **詳細ページの項目は分類ごとに違う。** 名前の違う同義の項目は同じキーへ寄せる
  (指定番号 / 登録番号 / 告示番号 → `designation_number` など)
- **CSV の列は分類によって意味が変わる** (101 の `重文指定年月日` 列は登録年月日、
  102 の `種別1` 列は国宝・重文区分)。CSV から採るのは緯度経度だけ
- **1 欄にカンマで詰められた値がある** (401 の `指定基準`)。`Kind.SPLIT` で割って
  配列にする。基準の文言そのものの区切りは読点なので割られない
- 詳細ページの HTML は**ラベルが数値文字参照で書かれている**ことがある
  (本文側の解説文の見出し)。正規表現ではなく HTML パーサで読む
- 解説文は本文側の欄とモーダルの `textarea` の両方にある。改行が残るのは後者
- **想定外は捨てずに `BuildReport` へ。** 未知のラベル・読めない日付・CSV と
  食い違う名称・所在地から決めた都道府県は実行のたび報告に出る
- テストの詳細ページはキーワード引数で組み立てない。Python は識別子を NFKC で
  正規化するので `種別１` (全角) が `種別1` に化ける
- **出力先の対応表は `catalog.py` の `TARGET_DATASETS` が正本** (ADR 0009 / ADR 0012)。
  振り分けは `datasets_of()` で、**複数のリポジトリを返しうる** (401 の複合指定)。
  どのキーの値で振り分けるかは `record.py` の `ROUTING_KEYS` が持つ。
  `--output-dir` はデータリポジトリを並べた親ディレクトリで、その配下に
  `<リポジトリ名>/data/<都道府県コード>_<ローマ字>.jsonl` を書く
- **0 件の県にファイルは置かない。** 行が全部無くなったファイルは消すが、**消すのは
  0 件だと断言できる実行だけ** (#57) — 全域を対象にし、詳細の取りこぼしが無く、
  その種別に 1 件以上書いた実行に限る。地域を絞った実行や取りこぼした実行では
  「取れていないだけ」と区別が付かないので報告に留める (`report.stale_files`)
- 既定の `data` は gitignore 済み。**ローカルではここからデータリポジトリ群
  (`~/Repos/bunkazai`) へ symlink を張ってある** ので、クローラーの手元から
  データを辿れて `--output-dir` も要らない。symlink も追跡されないため、
  データリポジトリは別リポジトリのまま (submodule にはしない)。
  **worktree にはこの symlink が無い** — 追跡されていないものは worktree に
  複製されないので、そのまま `build-records` を走らせると worktree の中に
  59 MB を書き出して**データリポジトリには 1 バイトも届かない**。worktree で
  出力を作るときは `ln -s ~/Repos/bunkazai data` を先に張る
- **各データリポジトリのルートに `meta.json` を書く** (ADR 0014)。出典表記と
  **利用日**・表示名・語彙・件数を機械可読で持つ。利用日は詳細を取得した日
  (`DetailEntry.fetched_at` の最大値) を**日本時間で切る** — 組み立てを走らせた日
  ではない。UTC のまま切ると日本の夕方以降のぶんが 1 日ずれる。
  表示名は分類ごとの原文ラベルの実測で、対応表に無いキーは `record.py` の
  `DERIVED_LABELS` が持つ。**生成物は決定的** (同じ入力なら同じバイト列) にして、
  データが変わらない月に差分を立てない

## 検証コマンド

Python (ADR 0003)。CI と同じ内容をローカルで回す。

```
pip install -e '.[dev]'   # 初回のみ
ruff check .
mypy src
pytest -q
./scripts/check-freshness.sh
```

CI (`.github/workflows/ci.yml`) ではこれを Python のマトリクスで実行する。
**PR では最小サポートの 3.12 だけ、main への push では 3.12 / 3.13 / 3.14 の 3 本**
— Actions の課金はジョブ単位の分切り上げで、20 秒で終わるこのジョブに 3 本 × 1 分が
引かれていたため。バージョン差は main への push と週次 (`weekly.yml`) で見る。

PR タイトルとドキュメントの検査は `.github/workflows/pr-policy.yml` が受け持つ。
`scripts/check-pr-title.sh` で Conventional Commits 形式を (squash merge でタイトルが
そのまま main のコミットメッセージになるため)、`scripts/check-freshness.sh` で
ドキュメントの参照ドリフト (ADR の連番と 4 節構成、本文が参照する ADR 番号・
リポ内パス・Issue) を検査する。判定スクリプト自体のテストも同じジョブで回す。
**CI と分けてあるのは、PR タイトルの検査だけが `edited` を拾う必要があるから** —
ci.yml で拾うとタイトルを直すたびに matrix が丸ごと再走する。参照ドリフトは
`.github/workflows/freshness.yml` で月次にも検査する — Issue の状態はコミット無しに
変わるため。

**CI からデータベースへアクセスするテストは置かない。** 外部サイトに依存する
テストは不安定なうえ、相手先に不要な負荷をかける。

データベースへ出るワークフローは 2 つだけで、どちらも検査ではない。

- `.github/workflows/weekly.yml` — 週次の差分更新 (取得そのものが目的)。
  毎週月曜 03:00 JST。push には code4heritage org の GitHub App が要る
  (secret は `DATA_PUSH_CLIENT_ID` と `DATA_PUSH_PRIVATE_KEY`)。**差分が無い週も
  `status.json` のコミットが 1 つ立ち、失敗したら Issue が立つ** — 本文は全部
  押した / 一部だけ押せた / 押す前に落ちたの 3 通りに書き分け、サイトの起動で
  落ちてもジョブは赤くしない (ADR 0023 / ADR 0029 / #69)。
  **26 リポジトリのうち 1 つ押せなくても残りは押す** (ADR 0029) — 一時的に弾かれた
  だけなら 3 回粘り、駄目なら記録して次へ進む。止めていた頃は、**2 番目が
  20 秒弾かれただけで残る 24 が試みられもしなかった** (2026-09-20 の週次。#98)。
  **前回の台帳は artifact で持ち回り**、
  CSV 同士で突き合わせる (ADR 0020)。`audit-listing --recover` は CSV に変化が
  あった週だけ挟む — 都道府県が空の行は回収しないと網羅性が確かめられず、消えた
  指定を落とせなくなる (ADR 0017)。**README の件数表のドリフト検査もここに置く**
  (`render-readme --check`)。`data` は追跡していないので PR の CI にはデータが
  無く、ここでしか確かめられない。ずれても**ジョブは失敗させず** Issue に残す
  (データの push は成功しているため)。最後に **heritages を起こす** — **確認日は
  渡さない**。サイトはデータリポジトリの `status.json` から読む (ADR 0028)
- `.github/workflows/reachability.yml` — 疎通確認。`workflow_dispatch` のみ
  (押したときしか走らない)。週次更新が使う 3 経路 (台帳の CSV・検索結果一覧・
  詳細ページ) を確かめる。既定は 126 件の 103

**Actions からも取れる** (2026-08-12 実測。3 経路とも通り、エラーページ 0 件)。
ADR 0006 で未検証だった「CloudFront 配下の WAF がデータセンター IP を弾くか」は
これで解消した。103 の全 51 地域が約 60 秒で、レートの上限も守られている
(1 地域 = 検索 + CSV の 2 リクエストが約 2 秒間隔)。ローカルの「204 リクエスト /
約 30 分」の大半は 101 (14,748 件) の CSV 生成 (サーバ側の処理時間) で、
**台帳を毎月取り直してもジョブ上限 6 時間には遠い**。

```
gh workflow run reachability.yml
```
